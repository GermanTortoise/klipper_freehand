# klipper_freehand

This allows you to control a Klipper 3D printer using your `wasd` keys and make it print, like how an Etch A Sketch works. The Klipper instance must run on a machine with a keyboard and screen, ie, a laptop.